apiVersion: v1
kind: Service
metadata:
  name: {{ include "music-assistant-alexa-api.fullname" . }}
  labels:
    {{- include "music-assistant-alexa-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "music-assistant-alexa-api.selectorLabels" . | nindent 4 }}
